# AI-Blueking 项目 AI 编码规则

> 本文件为 AI 编码助手提供项目上下文，帮助 AI 理解架构设计和编码规范。

---

## 1. 项目概述

### 1.1 项目定位

AI-Blueking（小鲸）是蓝鲸智云的 AI 聊天组件库，提供：
- **AIBlueking** - 完整版组件（含悬浮球、拖拽、弹窗）
- **ChatBot** - 核心聊天组件（可独立嵌入）

### 1.2 技术栈

- **框架**: Vue 3 + Composition API
- **语言**: TypeScript（严格模式）
- **状态管理**: Vue Reactivity（ref/computed/shallowRef）
- **SDK**: @blueking/chat-helper（AG-UI SDK）
- **UI 组件**: @blueking/chat-x

---

## 2. 架构分层

```
┌─────────────────────────────────────────────────────────────────┐
│                     UI 层 (Vue Components)                       │
│  ai-blueking.vue, chat-bot.vue, ChatInput, MessageRender         │
│  职责：渲染 UI、处理用户交互、协调数据转换                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    业务管理层 (Managers)                          │
│  ChatBusinessManager, SessionBusinessManager, ShortcutManager     │
│  职责：编排业务流程、管理业务状态、不直接操作数据                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    数据层 (chat-helper SDK)                       │
│  useAgent, useMessage, useSession, http modules                   │
│  职责：数据获取、HTTP 请求、数据转换、协议处理                       │
└─────────────────────────────────────────────────────────────────┘
```

### 2.1 各层职责边界

| 层级 | 职责 | 禁止 |
|------|------|------|
| **UI 层** | 渲染、用户交互、UI→API 数据转换 | 包含复杂业务逻辑、直接调用 HTTP |
| **业务管理层** | 业务流程编排、状态管理 | 直接操作 DOM、关心具体数据格式 |
| **数据层** | 数据获取、HTTP 请求、数据转换 | 包含业务逻辑、UI 相关代码 |

---

## 3. 核心模块说明

### 3.1 ChatBusinessManager

**文件**: `src/manager/business/chat-business-manager.ts`

**职责**:
- 封装聊天流程
- 管理聊天状态（isGenerating、citeText）
- 调用 agent.chat() 发送消息

**关键 API**:
```typescript
class ChatBusinessManager {
  get messages(): Ref<IMessage[]>;
  get isGenerating(): Ref<boolean>;
  
  async sendMessage(content: string, sessionCode: string, options?: SendMessageOptions): Promise<void>;
  stopGeneration(): void;
  setCiteText(text: string): void;
}
```

### 3.2 SessionBusinessManager

**文件**: `src/manager/business/session-business-manager.ts`

**职责**:
- 管理会话列表和当前会话
- 处理会话创建、切换、删除

### 3.3 ShortcutManager

**文件**: `src/manager/business/shortcut-manager.ts`

**职责**:
- 管理快捷方式列表
- 处理快捷方式过滤

### 3.4 ChatBot 组件

**文件**: `src/components/chat-bot.vue`

**职责**:
- 渲染聊天界面
- 处理用户输入
- 协调快捷指令、引用等功能

**使用模式**:
- **独立模式**: 传入 `url`，组件内部创建 chatHelper
- **集成模式**: 传入 `chatHelper`，复用父组件实例

---

## 4. 数据流规范

### 4.1 消息发送流程

```
用户点击发送
    │
    ▼
ChatBot.handleSendMessage() / handleShortcutSubmit()
    │
    ├── 构建 property（如有引用/快捷指令）
    │
    ▼
ChatBot.doSendMessage(message, options)
    │
    ├── 校验 chatBusinessManager 和 sessionCode
    ├── 清空输入框和引用
    ├── emit('send-message')
    │
    ▼
ChatBusinessManager.sendMessage(content, sessionCode, options)
    │
    ├── 处理 citeText（如有）
    ├── 设置 isGenerating = true
    │
    ▼
AgentModule.chat(content, sessionCode, url, config, property)
    │
    ▼
HTTP 请求 → 后端 → 流式响应 → 消息列表更新
```

### 4.2 Property 结构规范

**引用内容**:
```typescript
{
  extra: {
    cite: "引用的文本内容"
  }
}
```

**快捷指令**:
```typescript
{
  extra: {
    cite: {
      type: "structured",
      title: "指令名称",
      data: [
        { key: "字段标签", value: "字段值" }
      ]
    },
    command: "指令 ID",
    context: [
      {
        [fieldKey]: fieldValue,
        context_type: "text" | "textarea",
        __label: "字段标签",
        __key: "字段键名",
        __value: "字段值"
      }
    ]
  }
}
```

---

## 5. 编码规范

### 5.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| Vue 组件文件 | PascalCase | `ChatBot.vue`, `AIHeader.vue` |
| 普通 TS/JS 文件 | kebab-case | `chat-business-manager.ts` |
| 文件夹 | kebab-case | `business/`, `ai-header/` |
| 类名 | PascalCase | `ChatBusinessManager` |
| 接口/类型 | I 前缀 + PascalCase | `IMessage`, `ISession` |
| 变量/函数 | camelCase | `sendMessage`, `userInput` |
| 常量 | UPPER_SNAKE_CASE | `MAX_COUNT`, `DEFAULT_URL` |

### 5.2 TypeScript 规范

**必须**:
- 所有公共 API 必须有完整类型定义
- Props、Emits、Expose 必须使用类型注解
- 导出的函数必须声明参数和返回值类型

**禁止**:
```typescript
// ❌ 禁止使用 any
const data: any = response;

// ❌ 禁止隐式 any
function handle(params) { }

// ❌ 禁止 @ts-ignore
// @ts-ignore
const result = someFunction();
```

**正确做法**:
```typescript
// ✅ 明确的类型定义
interface CreateSessionOptions {
  name?: string;
  sessionCode?: string;
}

async function createSession(options: CreateSessionOptions): Promise<void> {
  // ...
}

// ✅ 使用 unknown 替代 any
function handleError(error: unknown): void {
  if (error instanceof Error) {
    console.error(error.message);
  }
}
```

### 5.3 Vue 组件规范

**Props 定义**:
```typescript
interface ComponentProps {
  /** 会话名称 */
  sessionName?: string;
  /** 是否启用 */
  enabled?: boolean;
}

const props = withDefaults(defineProps<ComponentProps>(), {
  sessionName: '',
  enabled: true,
});
```

**Emits 定义**:
```typescript
interface ComponentEmits {
  (e: 'update:value', value: string): void;
  (e: 'session-change', sessionCode: string): void;
}

const emit = defineEmits<ComponentEmits>();
```

**Expose 定义**:
```typescript
interface ComponentExpose {
  focus: () => void;
  getValue: () => string;
}

defineExpose<ComponentExpose>({
  focus,
  getValue,
});
```

---

## 6. 设计原则

### 6.1 低冗余（DRY）

**禁止**:
```typescript
// ❌ 相同逻辑多处重复
// 文件 A
const sendMsg = async () => {
  if (!chatBusinessManager) return;
  const sessionCode = chatHelper.session.current?.value.sessionCode;
  if (!sessionCode) return;
  await chatBusinessManager.sendMessage(content, sessionCode);
};

// 文件 B（重复的校验逻辑）
const submitForm = async () => {
  if (!chatBusinessManager) return;
  const sessionCode = chatHelper.session.current?.value.sessionCode;
  if (!sessionCode) return;
  await chatBusinessManager.sendMessage(content, sessionCode);
};
```

**正确做法**:
```typescript
// ✅ 抽取公共方法
const doSendMessage = async (message: string, options = {}) => {
  if (!chatBusinessManager || !chatHelper) {
    throw new Error('chatBusinessManager not initialized');
  }
  const sessionCode = chatHelper.session.current?.value.sessionCode;
  if (!sessionCode) {
    throw new Error('no active session');
  }
  await chatBusinessManager.sendMessage(message, sessionCode, options);
};

// 调用方
await doSendMessage(content);
await doSendMessage(content, { property });
```

### 6.2 高内聚（Single Responsibility）

每个模块/类/函数应只有一个明确的职责：

| 模块类型 | 职责范围 | 禁止 |
|---------|---------|------|
| Composable | 可复用的状态逻辑 | 包含 UI 渲染逻辑 |
| BusinessManager | 业务流程编排 | 直接操作 DOM |
| Component | UI 渲染和用户交互 | 包含复杂业务逻辑 |
| Utils | 纯函数工具 | 依赖外部状态 |

### 6.3 低耦合（Loose Coupling）

```typescript
// ❌ 组件直接依赖 SDK 细节
const handleClick = async () => {
  await chatHelper.session.createSession({
    sessionCode: `session_${Date.now()}`,
    sessionName: '新会话',
  });
  await chatHelper.session.getSessions();
  await chatHelper.session.chooseSession(sessionCode);
};

// ✅ 通过业务管理器抽象
const handleClick = async () => {
  await sessionBusinessManager.createNewSession();
};
```

### 6.4 不过度设计（YAGNI）

- 只实现当前需要的功能
- 不为假设的未来需求创建抽象
- 单一场景的逻辑可以放在组件内，不必强行抽取

```typescript
// ❌ 过度设计：为单一场景创建复杂抽象
class ShortcutPropertyBuilder {
  private shortcut: Shortcut;
  private formModel: Record<string, unknown>;
  
  constructor(shortcut: Shortcut) { ... }
  setFormModel(formModel: Record<string, unknown>) { ... }
  build() { ... }
}

// ✅ 适度设计：简单函数即可
const buildShortcutProperty = (shortcut: Shortcut, formModel: Record<string, unknown>) => {
  return {
    extra: {
      cite: { ... },
      command: shortcut.id,
      context: [ ... ],
    },
  };
};
```

---

## 7. 新功能开发指南

### 7.1 添加新的业务逻辑

1. **确定逻辑归属**
   - UI 逻辑 → 放在组件内
   - 业务逻辑 → 放在 BusinessManager
   - 可复用逻辑 → 抽取为 Composable

2. **遵循分层原则**
   - 组件只调用 Manager 的公开方法
   - Manager 只调用 SDK 的模块方法

3. **类型定义**
   - 在 `types.ts` 中定义相关类型
   - 在函数/方法上添加完整类型注解

### 7.2 添加新的消息属性（Property）

1. 在 `@blueking/chat-helper` 的 `IMessageProperty` 类型中添加
2. 在 `SendMessageOptions` 中使用 `property` 字段
3. 在组件中构建 property 并传递给 `sendMessage`

### 7.3 代码审查清单

- [ ] 新功能符合现有架构设计
- [ ] 没有引入重复逻辑（检查是否可抽取公共方法）
- [ ] 模块职责清晰，没有越界调用
- [ ] 函数/方法职责单一，长度适中（建议 < 50 行）
- [ ] 没有使用 `any` 类型
- [ ] 公共 API 有完整类型定义
- [ ] 关键逻辑有注释说明
- [ ] 错误处理完善

---

## 8. 常见模式

### 8.1 组件内发送消息

```typescript
// chat-bot.vue 中的标准模式
const doSendMessage = async (message: string, options: { property?: Record<string, unknown> } = {}) => {
  if (!chatBusinessManager || !chatHelper) {
    throw new Error('[ChatBot] Cannot send message: not initialized');
  }

  const sessionCode = chatHelper.session.current?.value.sessionCode;
  if (!sessionCode) {
    throw new Error('[ChatBot] Cannot send message: no active session');
  }

  userInput.value = [[]];
  cite.value = '';
  emit('send-message', message);
  
  await chatBusinessManager.sendMessage(message, sessionCode, options);
  scrollToBottom();
};
```

### 8.2 处理工具操作

```typescript
const handleToolAction = async (tool: IToolBtn, message: IMessage) => {
  // 特殊工具处理
  if (tool.id === 'cite') {
    cite.value = typeof message.content === 'string' ? message.content : '';
    focusInput();
    return;
  }

  // 通用工具处理
  emit('tool-action', tool, message);
  if (props.onToolAction) {
    return props.onToolAction(tool, message);
  }
};
```

### 8.3 构建快捷指令 Property

```typescript
const buildShortcutProperty = (shortcut: Shortcut, formModel: Record<string, unknown>) => {
  const components = shortcut.components || [];

  return {
    extra: {
      cite: {
        type: 'structured',
        title: shortcut.name,
        data: components.map(c => ({
          key: c.name || c.key,
          value: String(formModel[c.key] ?? ''),
        })),
      },
      command: shortcut.id,
      context: components.map(c => ({
        [c.key]: formModel[c.key],
        context_type: c.type,
        __label: c.name || c.key,
        __key: c.key,
        __value: formModel[c.key],
      })),
    },
  };
};
```

---

## 9. 相关文档

- `ARCHITECTURE.md` - 详细架构设计文档
- `README.md` - 使用指南和 API 文档
- `CHANGELOG.md` - 版本更新日志
- `examples/` - 使用示例

---

*文档版本: 1.0.0*
*最后更新: 2026-01-16*
